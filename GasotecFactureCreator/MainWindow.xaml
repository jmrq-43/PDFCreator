<Window x:Class="GasotecFactureCreator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GasotecFactureCreator"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid Background="#D9D9D9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Background=" #0F0788">
            <TextBlock
                FontSize="50"
                Foreground=" #ffff"
                TextAlignment="Center"
                Margin="10">
                Generador de facturas gasotec
            </TextBlock>
        </Grid>
        <StackPanel Grid.ColumnSpan="1" Grid.RowSpan="2">
            <Frame Grid.Row="1" Name="MainFrame" NavigationUIVisibility="Hidden"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,100,0,0">
            <Button Width="140" Height="50" Background=" #0F0788" Click="Button_Go_Data_Collector">
                <TextBlock Foreground="#ffff" FontSize="30" Text="Empezar" />
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="buttonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="30">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#7F79D4"></Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>